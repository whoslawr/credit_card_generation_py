# credit_card_generation_py
The card generation algorithm creates valid credentials for credit cards,based on some rules.
The luhn algorithms is used to verify if a credit card is valid (based on its number).
